# Gemini 开发准则

- **分析先行**：在进行任何代码修改前，必须先深入分析问题的根本原因。
- **TDD 原则**：每一次代码修改都必须严格遵循测试驱动开发（TDD）流程。即：必须先编写能够复现问题或定义新功能的测试用例（并确认测试失败），然后再编写代码让测试通过。
- **全面覆盖**：测试用例应覆盖尽可能广泛的场景（包括正常路径、异常路径、边界条件），并永久添加到项目的测试库中（如 `tests/` 目录），禁止用完即删。
- **全量回归**：完成任何开发任务后，必须运行整个项目的测试套件（执行 `npm test`），确保修改没有引入回归错误或破坏现有功能。
- **语言要求**：始终使用简体中文回答，包括内部推理、执行计划以及任务描述。